---
title: 工具搜索
sidebar_position: 95
---

# 工具搜索

当一个会话附加了许多 MCP 服务器或非核心插件工具时，它们的 JSON 模式可能会在每一轮对话中消耗相当一部分上下文窗口——即使其中只有少数几个与用户实际需求相关。

**工具搜索**是 Hermes 针对此问题的可选渐进式披露层。激活后，模型可见的工具数组中的 MCP 和插件工具将被三个桥接工具取代，模型按需加载每个具体工具的模式。

:::info 内置 Hermes 工具永不延迟
构成 Hermes 核心能力集的工具（`terminal`、`read_file`、`write_file`、`patch`、`search_files`、`todo`、`memory`、`browser_*`、`web_search`、`web_extract`、`clarify`、`execute_code`、`delegate_task`、`session_search`、`send_message` 以及 `_HERMES_CORE_TOOLS` 中的其余工具）*始终*直接加载。只有 MCP 工具和非核心插件工具才有资格被延迟加载。
:::

## 工作原理

当工具搜索在某一轮对话中激活时，模型会看到三个新工具，取代了被延迟加载的工具：

```
tool_search(query, limit?)     — 搜索延迟工具目录
tool_describe(name)            — 加载单个工具的完整模式
tool_call(name, arguments)     — 调用一个延迟工具
```

典型的交互如下所示：

```
模型：tool_search("create a github issue")
  → { matches: [{ name: "mcp_github_create_issue", ... }, ...] }
模型：tool_describe("mcp_github_create_issue")
  → { parameters: { type: "object", properties: { ... } } }
模型：tool_call("mcp_github_create_issue", { title: "...", body: "..." })
  → { ok: true, issue_number: 42 }
```

当模型调用 `tool_call` 时，Hermes **解包桥接工具**并分发底层工具，就像模型直接调用它一样。工具调用前的钩子、防护栏、确认提示词以及工具调用后的钩子都针对真实的工具名称运行——而不是针对 `tool_call`。CLI 和消息网关中的活动流也会解包，因此您看到的是底层工具，而不是桥接工具。

## 何时激活？

默认情况下，工具搜索以 `auto` 模式运行：只有当可延迟工具模式至少会消耗活动模型上下文窗口的 10% 时，它才会激活。低于此阈值时，工具数组的组装是纯直通的，您无需支付任何开销。

每次构建工具数组时都会重新评估此决定，因此：

- 一个只有少量 MCP 工具且使用长上下文模型的会话永远不会激活工具搜索。
- 一个附加了许多 MCP 服务器（通常 15+ 个工具）的会话会开始激活它。
- 在会话中途移除 MCP 服务器后，下一次组装时会正确恢复到直接暴露模式。

## 配置

```yaml
tools:
  tool_search:
    enabled: auto       # auto（默认）、on 或 off
    threshold_pct: 10   # 上下文百分比 — 仅在 auto 模式下使用
    search_default_limit: 5
    max_search_limit: 20
```

| 键 | 默认值 | 含义 |
| --- | --- | --- |
| `enabled` | `auto` | `auto` 在超过阈值时激活；`on` 只要存在至少一个可延迟工具就始终激活；`off` 完全禁用。 |
| `threshold_pct` | `10` | `auto` 模式激活时的上下文长度百分比。范围 0–100。 |
| `search_default_limit` | `5` | 当模型调用 `tool_search` 而未指定 `limit` 时返回的匹配项数量。 |
| `max_search_limit` | `20` | 模型可以通过 `limit` 请求的硬性上限。范围 1–50。 |

您也可以使用传统的布尔形式：

```yaml
tools:
  tool_search: true   # 等同于 {enabled: auto}
```

## 何时不应使用

工具搜索以固定的每轮 Token 成本（三个桥接工具模式，约 300 个 Token）和至少一次额外的往返（搜索 → 描述 → 调用）为代价，换取延迟加载模式节省的开销。当您拥有许多工具且每轮只使用少数几个时，这是明显的优势；当您总共只有少量工具时，这就是开销。

默认的 `auto` 模式会为您处理这一点。如果您无条件地设置 `enabled: on`，请预期在小型工具集上会有轻微的每轮成本。

## 无法消除的权衡

这些源于提示词缓存完整性不变性——它们是任何渐进式披露设计固有的，并非此特定实现所独有：

- **冷工具的额外往返。** 模型首次需要一个延迟工具时，会花费一两次额外的模型调用来查找和加载其模式。静态端节省的 Token 是真实的，但一部分会在运行时偿还。
- **延迟模式无法从缓存中受益。** 加载的 `tool_describe` 结果会进入对话历史（因此确实会在后续轮次中被缓存），但它永远无法从系统提示词缓存前缀中受益。
- **模型质量依赖性。** 工具搜索假设模型能够为其想要的工具编写合理的搜索查询。较小的模型在这方面表现较差；已发布的 Anthropic 数据（Opus 4 模型在使用与不使用工具搜索时，准确率从 49% 提升到 74%）显示了优势，但也表明仍有约 26 个百分点的准确率损失源于检索失败。
- **工具集编辑使缓存失效。** 在会话中途添加或移除工具会改变桥接工具的描述（其中包含延迟工具的数量）和目录，因此提示词缓存会失效。这与任何工具集编辑的权衡相同。

## 实现细节

- **检索：** 基于词元化的工具名称 + 描述 + 参数名称的 BM25 算法。当 BM25 未返回任何正分数匹配项时，回退到对工具名称进行字面子串匹配，这可以防止零 IDF 退化情况（例如，在目录中每个工具名称都包含 "github" 时搜索 `"github"`）。
- **目录在轮次间是无状态的。** 每次组装时都从当前工具定义列表重建——没有会话键控的 `Map`。这避免了存储的目录与实时工具注册表不同步这类错误。
- **目录的作用域限定在会话的工具集内。** `tool_search`、`tool_describe` 和 `tool_call` 只能看到并调用会话实际被授予的工具。一个子 Agent、看板工作器或消息网关会话如果被限制在工具集的子集内，则无法使用桥接工具来发现或调用该子集之外的工具——延迟目录是会话自身启用/禁用工具集中可延迟的部分，而不是整个进程注册表。
- **没有 JS 沙盒。** Hermes 使用更简单的“结构化工具”模式（搜索 / 描述 / 调用作为普通函数）。其他一些实现提供的 JS 沙盒“代码模式”涉及较大的攻击面；我们跳过了它。

## 另请参阅

- `tools/tool_search.py` — 实现
- `tests/tools/test_tool_search.py` — 回归测试套件
- 原始实现 PR 中的 `openclaw-tool-search-report` PDF 文件，其中包含了影响设计的研究